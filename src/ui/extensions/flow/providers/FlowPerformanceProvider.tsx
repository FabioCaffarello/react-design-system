/**
 * Flow Performance Provider
 * 
 * Provider for performance monitoring and optimization.
 * Re-exports FlowPerformanceProvider from context for convenience.
 */

/* eslint-disable react-refresh/only-export-components */
export { FlowPerformanceProvider, useFlowPerformanceContext } from '../context/FlowPerformanceContext';
export type { FlowPerformanceProviderProps, FlowPerformanceContextValue } from '../context/FlowPerformanceContext';
